# 企业 OA 测试计划

## 内部知识分享增量（已完成）

### 后端

- API 正常流程：分类、创建、修改、发布、撤回、列表、详情、我的文章。
- 状态与归属：重复发布/撤回、撤回终态、非作者修改/发布/撤回。
- 可见性：他人草稿和撤回文章返回 404，已发布文章可见。
- 查询：标题搜索、分类筛选、状态筛选、分页 total 与 items 一致。
- 认证：未登录 401、强制改密 403、管理员 403、停用员工 401。
- 隔离：使用 RUN_ID 创建数据并精确清理，不执行无条件 deleteMany。

### 前端

- API 路径、参数和请求体。
- 菜单与路由可达性。
- 状态到操作按钮的映射。
- 表单校验、空状态和错误反馈。

### 回归与交付门禁

后端和前端全量测试、两端构建、Prisma Migration 状态、`git diff --check`。浏览器验证完整文章流程和关键权限场景。测试结果只能在实际执行后写入测试报告。

## 知识社区增强测试计划（Day 9 已执行）

按五阶段验证正常、异常、归属、状态、并发、停用会话和跨入口内容隔离。重点覆盖评论越权、点赞收藏唯一性、下架复审绕过、AI 人工确认、无密钥模拟、来源真实性和提示注入边界。

### 执行结果

- 评论模块：正常发表、越权删除、分页、软删除 — 通过
- 点赞收藏：幂等性、取消、我的收藏、失效占位 — 通过
- 内容管理：下架附原因、审核通过/驳回、分类CRUD、管理员删评论 — 通过
- AI 写作：草稿、润色、摘要、人工确认流程 — 通过
- AI 查询：回答+来源、无依据反馈、无密钥503 — 通过
- 安全：提示注入边界、频率限制、停用会话隔离 — 通过
- 回归：后端全量测试通过，前端构建通过，Migration 状态正常
